export function BacktestParamsStep() {
  return (
    <div className="space-y-6">
      <h2 className="text-lg" style={{ color: '#0B236B' }}>Backtest Parameters</h2>
      <p className="text-sm text-gray-600">
        Configure the backtest parameters (dates, pricing, corporate actions, etc.)
      </p>
    </div>
  );
}
